applications:
  ambassador-auth:
    options:
      password: foobar
  mariadb:
    options:
      root-password: foobar
  pipelines-api:
    options:
      mysql-root-password: foobar
      minio-secret-key: minio123
