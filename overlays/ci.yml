applications:
  ambassador-auth:
    options:
      password: foobar
  katib-db:
    options:
      root-password: foobar
      database: katib
  mariadb:
    options:
      root-password: foobar
  pipelines-api:
    options:
      minio-secret-key: minio123
